<%= simple_form_for(@app) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :app_dictionary_entry %>
    <%= f.association :cloud %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
